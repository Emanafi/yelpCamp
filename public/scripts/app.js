document.addEventListener("DOMContentLoaded", function(event) {
	
	//global variables
	const searchItem = document.getElementById('filter-submit');
	const submitButton = document.getElementById('search');
	const baseURL = 'http://localhost:3000';

	//get 


	//event listeners
	submitButton.addEventListener('submit', /*callbackfunction*/)
});